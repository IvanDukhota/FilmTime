version: '3.9'
services:
  web:
    build: .
    env_file:
      - .env
    expose:
      - "8000:8000"
    volumes:
      - .:/app
  nginx:
    build: ./nginx
    ports:
      - 80:80
    depends_on:
      - web
